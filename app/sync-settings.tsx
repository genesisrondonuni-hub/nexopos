import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import AsyncStorage from "@react-native-async-storage/async-storage";
import { router } from "expo-router";
import { useState } from "react";
import { ActivityIndicator, Alert, Pressable, ScrollView, StyleSheet, Text, View } from "react-native";

import { Card, colors, PrimaryButton, SoftButton } from "@/components/nexo-ui";
import { ScreenContainer } from "@/components/screen-container";
import { getLoginUrl, startOAuthLogin } from "@/constants/oauth";
import { useAuth } from "@/hooks/use-auth";
import { trpc } from "@/lib/trpc";
import { createOperationSnapshot, findSnapshotConflicts, OPERATION_SNAPSHOT_KEYS, parseOperationSnapshot, reconcileOperationSnapshots, type ReconciliationChoice, type SnapshotConflict } from "@/shared/operation-snapshot";

const BUSINESS_KEY = "principal";

export default function SyncSettingsScreen() {
  const { user, loading: authLoading, refresh } = useAuth();
  const [working, setWorking] = useState<"upload" | "restore" | "reconcile" | null>(null);
  const [conflicts, setConflicts] = useState<SnapshotConflict[]>([]);
  const [choices, setChoices] = useState<Record<string, ReconciliationChoice>>({});
  const snapshot = trpc.sync.get.useQuery({ businessKey: BUSINESS_KEY }, { enabled: Boolean(user) });
  const saveSnapshot = trpc.sync.save.useMutation();
  const buildLocal = async () => createOperationSnapshot(await AsyncStorage.multiGet(OPERATION_SNAPSHOT_KEYS));
  const login = async () => { if (!getLoginUrl()) { Alert.alert("Inicio de sesión no disponible", "La configuración de autenticación estará disponible al ejecutar la aplicación publicada."); return; } await startOAuthLogin(); void refresh(); };
  const upload = async () => {
    if (!user) return; setWorking("upload");
    try { const payload = JSON.stringify(await buildLocal()); if (payload.length > 750000) { Alert.alert("Respaldo muy grande", "Reduce imágenes o históricos locales antes de sincronizar."); return; } const result = await saveSnapshot.mutateAsync({ businessKey: BUSINESS_KEY, expectedRevision: snapshot.data?.revision ?? 0, payload }); await snapshot.refetch(); Alert.alert("Respaldo actualizado", `La operación de este equipo se guardó en la nube con versión ${result.revision}.`); }
    catch (error) { Alert.alert("No se pudo respaldar", error instanceof Error && error.message.includes("CONFLICT") ? "Hay cambios de otra caja. Usa Conciliar cambios antes de respaldar." : error instanceof Error ? error.message : "Verifica tu conexión e intenta de nuevo."); }
    finally { setWorking(null); }
  };
  const prepareReconciliation = async () => {
    if (!snapshot.data?.payload) { Alert.alert("Sin copia remota", "Crea primero un respaldo desde otra caja o este equipo."); return; }
    const remote = parseOperationSnapshot(snapshot.data.payload); if (!remote) { Alert.alert("Respaldo inválido", "La copia remota no tiene una estructura compatible."); return; }
    const detected = findSnapshotConflicts(await buildLocal(), remote);
    if (!detected.length) { Alert.alert("Sin conflictos", "No hay cambios simultáneos del mismo registro. Al conciliar se conservarán altas independientes de ambas cajas."); }
    setConflicts(detected); setChoices({});
  };
  const applyReconciliation = async () => {
    if (!user || !snapshot.data?.payload) return;
    const remote = parseOperationSnapshot(snapshot.data.payload); if (!remote) return;
    setWorking("reconcile");
    try { const merged = reconcileOperationSnapshots(await buildLocal(), remote, choices); await AsyncStorage.multiSet(merged.records); const payload = JSON.stringify(merged); const result = await saveSnapshot.mutateAsync({ businessKey: BUSINESS_KEY, expectedRevision: snapshot.data.revision, payload }); await snapshot.refetch(); setConflicts([]); Alert.alert("Cambios conciliados", `Se preservaron las altas independientes y se guardó la versión ${result.revision}. Reinicia NexoPOS para recargar los datos locales.`); }
    catch (error) { Alert.alert("No se pudo conciliar", error instanceof Error ? error.message : "Actualiza la copia remota e intenta nuevamente."); }
    finally { setWorking(null); }
  };
  const restore = async () => {
    if (!snapshot.data?.payload) { Alert.alert("Sin respaldo", "Todavía no hay una copia remota para este negocio."); return; }
    const parsed = parseOperationSnapshot(snapshot.data.payload); if (!parsed) { Alert.alert("Respaldo inválido", "La copia remota no tiene una estructura compatible."); return; }
    Alert.alert("Restaurar operación", "Se reemplazarán los datos locales por la copia remota. Después reinicia la aplicación.", [{ text: "Cancelar", style: "cancel" }, { text: "Restaurar", style: "destructive", onPress: async () => { setWorking("restore"); try { await AsyncStorage.multiSet(parsed.records); Alert.alert("Datos restaurados", `Copia creada el ${new Date(parsed.capturedAt).toLocaleString("es-VE")}. Cierra y abre NexoPOS para cargarla.`); } catch { Alert.alert("No se pudo restaurar", "Los datos locales no fueron modificados completamente. Intenta de nuevo."); } finally { setWorking(null); } }}]);
  };
  const lastBackup = snapshot.data?.updatedAt ? new Date(snapshot.data.updatedAt).toLocaleString("es-VE") : "Aún no hay respaldo";
  return <ScreenContainer edges={["top", "bottom", "left", "right"]} containerClassName="bg-[#F6F3EE]" className="px-5"><ScrollView contentContainerStyle={styles.content} showsVerticalScrollIndicator={false}><View style={styles.header}><Pressable onPress={() => router.back()} style={({ pressed }) => [styles.back, pressed && styles.pressed]}><MaterialIcons name="arrow-back" size={21} color={colors.ink} /></Pressable><View><Text style={styles.eyebrow}>DATOS DEL NEGOCIO</Text><Text style={styles.title}>Respaldo y sincronización</Text></View></View><Card style={styles.hero}><View style={styles.heroIcon}><MaterialIcons name="cloud-sync" size={25} color={colors.green} /></View><View style={styles.heroCopy}><Text style={styles.heroTitle}>Operación protegida</Text><Text style={styles.heroText}>Mantén una copia versionada de ventas, inventario, caja, CRM y proveedores. La operación local sigue disponible sin conexión.</Text></View></Card>{authLoading ? <ActivityIndicator color={colors.green} /> : !user ? <Card style={styles.account}><MaterialIcons name="lock-outline" size={21} color={colors.gold} /><View style={styles.accountCopy}><Text style={styles.accountTitle}>Cuenta requerida para respaldar</Text><Text style={styles.accountText}>Inicia sesión para vincular el respaldo a tu cuenta. Los datos no se exponen en el repositorio público.</Text></View><SoftButton label="Iniciar sesión" icon="login" onPress={() => void login()} /></Card> : <><Card style={styles.account}><View style={styles.userIcon}><MaterialIcons name="verified-user" size={19} color={colors.green} /></View><View style={styles.accountCopy}><Text style={styles.accountTitle}>{user.name || "Cuenta autenticada"}</Text><Text style={styles.accountText}>{user.email || "Respaldo vinculado"}</Text></View></Card><Card style={styles.status}><View><Text style={styles.statusLabel}>Última copia remota</Text><Text style={styles.statusValue}>{lastBackup}</Text></View><View style={styles.statusBadge}><Text style={styles.statusBadgeText}>V{snapshot.data?.revision ?? 0}</Text></View></Card><PrimaryButton label={working === "upload" ? "Guardando respaldo…" : "Respaldar este equipo"} icon="cloud-upload" disabled={Boolean(working) || saveSnapshot.isPending} onPress={() => void upload()} /><SoftButton label="Analizar y conciliar cambios" icon="merge-type" onPress={() => void prepareReconciliation()} /><SoftButton label={working === "restore" ? "Restaurando…" : "Restaurar desde la nube"} icon="cloud-download" onPress={() => void restore()} />{conflicts.length ? <Card style={styles.conflictCard}><Text style={styles.conflictTitle}>Conflictos por registro</Text><Text style={styles.conflictHelp}>Elige qué versión conservar para cada modificación simultánea. Las altas independientes se combinan automáticamente.</Text>{conflicts.slice(0, 8).map((conflict) => { const choice = choices[conflict.id] ?? "LOCAL"; return <View key={conflict.id} style={styles.conflictRow}><Text style={styles.conflictLabel}>{conflict.label}</Text><View style={styles.choiceRow}>{(["LOCAL", "REMOTE"] as const).map((entry) => <Pressable key={entry} onPress={() => setChoices((current) => ({ ...current, [conflict.id]: entry }))} style={[styles.conflictChoice, choice === entry && styles.conflictChoiceSelected]}><Text style={[styles.conflictChoiceText, choice === entry && styles.conflictChoiceTextSelected]}>{entry === "LOCAL" ? "Esta caja" : "Nube"}</Text></Pressable>)}</View></View>; })}{conflicts.length > 8 ? <Text style={styles.more}>Se resolverán {conflicts.length - 8} conflictos adicionales con la preferencia local hasta que se revise cada registro.</Text> : null}<PrimaryButton label={working === "reconcile" ? "Conciliando…" : "Aplicar conciliación"} icon="merge-type" disabled={Boolean(working)} onPress={() => void applyReconciliation()} /></Card> : null}</>}<Card style={styles.notice}><MaterialIcons name="info-outline" size={18} color={colors.gold} /><Text style={styles.noticeText}>La conciliación detecta cambios simultáneos por registro, conserva altas independientes y requiere una decisión explícita cuando el mismo registro fue modificado en dos cajas.</Text></Card></ScrollView></ScreenContainer>;
}

const styles = StyleSheet.create({ content: { gap: 11, paddingBottom: 28, paddingTop: 10 }, header: { alignItems: "center", flexDirection: "row", gap: 11, marginBottom: 4 }, back: { alignItems: "center", backgroundColor: colors.white, borderColor: colors.line, borderRadius: 13, borderWidth: 1, height: 42, justifyContent: "center", width: 42 }, eyebrow: { color: colors.muted, fontSize: 9, fontWeight: "900", letterSpacing: 0.8 }, title: { color: colors.ink, fontSize: 21, fontWeight: "800", marginTop: 2 }, hero: { alignItems: "flex-start", backgroundColor: colors.mint, borderColor: "#B5E0CF", flexDirection: "row", gap: 10, padding: 13 }, heroIcon: { alignItems: "center", backgroundColor: colors.white, borderRadius: 12, height: 42, justifyContent: "center", width: 42 }, heroCopy: { flex: 1 }, heroTitle: { color: colors.ink, fontSize: 13, fontWeight: "800" }, heroText: { color: colors.muted, fontSize: 10, lineHeight: 14, marginTop: 3 }, account: { alignItems: "center", flexDirection: "row", gap: 9, padding: 12 }, accountCopy: { flex: 1 }, accountTitle: { color: colors.ink, fontSize: 12, fontWeight: "800" }, accountText: { color: colors.muted, fontSize: 9, lineHeight: 13, marginTop: 2 }, userIcon: { alignItems: "center", backgroundColor: colors.mint, borderRadius: 10, height: 32, justifyContent: "center", width: 32 }, status: { alignItems: "center", flexDirection: "row", justifyContent: "space-between", padding: 13 }, statusLabel: { color: colors.muted, fontSize: 10, fontWeight: "800" }, statusValue: { color: colors.ink, fontSize: 12, fontWeight: "800", marginTop: 3 }, statusBadge: { alignItems: "center", backgroundColor: colors.mint, borderRadius: 999, minWidth: 36, paddingHorizontal: 8, paddingVertical: 6 }, statusBadgeText: { color: colors.green, fontSize: 10, fontWeight: "900" }, conflictCard: { gap: 9, padding: 12 }, conflictTitle: { color: colors.ink, fontSize: 13, fontWeight: "900" }, conflictHelp: { color: colors.muted, fontSize: 10, lineHeight: 14 }, conflictRow: { borderTopColor: colors.line, borderTopWidth: 1, gap: 6, paddingTop: 8 }, conflictLabel: { color: colors.ink, fontSize: 10, fontWeight: "800" }, choiceRow: { flexDirection: "row", gap: 6 }, conflictChoice: { backgroundColor: colors.white, borderColor: colors.line, borderRadius: 8, borderWidth: 1, paddingHorizontal: 9, paddingVertical: 6 }, conflictChoiceSelected: { backgroundColor: colors.mint, borderColor: colors.green }, conflictChoiceText: { color: colors.muted, fontSize: 9, fontWeight: "800" }, conflictChoiceTextSelected: { color: colors.green }, more: { color: colors.muted, fontSize: 9, lineHeight: 13 }, notice: { alignItems: "flex-start", backgroundColor: "#FFF8E8", borderColor: "#F0DCA1", flexDirection: "row", gap: 8, padding: 11 }, noticeText: { color: "#725117", flex: 1, fontSize: 10, lineHeight: 14 }, pressed: { opacity: 0.7, transform: [{ scale: 0.98 }] } });

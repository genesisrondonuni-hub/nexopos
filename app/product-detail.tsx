import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { router, useLocalSearchParams } from "expo-router";
import { FlatList, Image, Pressable, StyleSheet, Text, View } from "react-native";

import { Card, colors, formatCOP } from "@/components/nexo-ui";
import { ScreenContainer } from "@/components/screen-container";
import { useNexo } from "@/lib/pos-store";
import type { ProductMovement } from "@/shared/pos-types";

export default function ProductDetailScreen() {
  const { productId } = useLocalSearchParams<{ productId?: string }>();
  const { products, productMovements } = useNexo();
  const product = products.find((item) => item.id === productId);
  const movements = productMovements.filter((movement) => movement.productId === productId);
  if (!product) return <ScreenContainer edges={["top", "bottom", "left", "right"]} containerClassName="bg-[#F6F3EE]" className="px-5"><View style={styles.notFound}><MaterialIcons name="inventory-2" size={36} color={colors.muted} /><Text style={styles.notFoundText}>No encontramos este producto.</Text><Pressable onPress={() => router.back()} style={styles.returnButton}><Text style={styles.returnText}>Volver al inventario</Text></Pressable></View></ScreenContainer>;
  const atMinimum = product.stock <= product.minStock;
  const renderMovement = ({ item }: { item: ProductMovement }) => <View style={styles.movement}><View style={[styles.movementIcon, item.quantityDelta && item.quantityDelta < 0 ? styles.outIcon : styles.inIcon]}><MaterialIcons name={item.type === "VENTA_POS" ? "point-of-sale" : item.type === "IMPORTACIÓN" ? "file-upload" : item.type === "REVERSIÓN" ? "undo" : "tune"} size={18} color={item.quantityDelta && item.quantityDelta < 0 ? colors.coral : colors.green} /></View><View style={styles.movementCopy}><Text style={styles.movementTitle}>{item.label}</Text><Text style={styles.movementDate}>{item.createdAt}</Text></View><View style={styles.movementEnd}>{item.quantityDelta ? <Text style={[styles.delta, item.quantityDelta < 0 && styles.deltaOut]}>{item.quantityDelta > 0 ? "+" : ""}{item.quantityDelta} ud.</Text> : null}{typeof item.stockAfter === "number" ? <Text style={styles.balance}>{item.stockAfter} en stock</Text> : null}</View></View>;
  return <ScreenContainer edges={["top", "bottom", "left", "right"]} containerClassName="bg-[#F6F3EE]" className="px-5"><FlatList data={movements} renderItem={renderMovement} keyExtractor={(item) => item.id} contentContainerStyle={styles.content} showsVerticalScrollIndicator={false} ListEmptyComponent={<View style={styles.empty}><MaterialIcons name="history" size={29} color={colors.muted} /><Text style={styles.emptyText}>Aún no hay movimientos registrados para este producto.</Text></View>} ListHeaderComponent={<><View style={styles.header}><Pressable onPress={() => router.back()} style={({ pressed }) => [styles.back, pressed && styles.pressed]}><MaterialIcons name="arrow-back" size={21} color={colors.ink} /></Pressable><Text style={styles.headerTitle}>Detalle de producto</Text><Pressable onPress={() => router.push({ pathname: "/product-editor", params: { productId: product.id } } as never)} style={({ pressed }) => [styles.edit, pressed && styles.pressed]}><MaterialIcons name="edit" size={19} color={colors.green} /></Pressable></View><Card style={styles.hero}><View style={styles.photo}>{product.imageUri ? <Image source={{ uri: product.imageUri }} style={styles.image} /> : <MaterialIcons name="inventory-2" size={33} color={colors.green} />}</View><View style={styles.heroCopy}><Text style={styles.code}>{product.code}</Text><Text style={styles.name}>{product.name}</Text><Text style={styles.description}>{product.description}</Text><Text style={styles.category}>{product.category}</Text></View></Card><View style={styles.metrics}><Card style={styles.metric}><Text style={styles.metricLabel}>Existencias</Text><Text style={[styles.metricValue, atMinimum && styles.lowStock]}>{product.stock} <Text style={styles.metricUnit}>ud.</Text></Text><Text style={styles.metricHint}>Mínimo: {product.minStock}</Text></Card><Card style={styles.metric}><Text style={styles.metricLabel}>Precio de venta</Text><Text style={styles.metricPrice}>{formatCOP(product.price)}</Text><Text style={styles.metricHint}>Costo: {formatCOP(product.cost)}</Text></Card></View><View style={styles.status}><MaterialIcons name={product.showInCatalog ? "visibility" : "visibility-off"} size={18} color={product.showInCatalog ? colors.green : colors.muted} /><Text style={styles.statusText}>{product.showInCatalog ? "Visible en el catálogo público" : "Oculto del catálogo público"}</Text></View><Text style={styles.sectionTitle}>Historial operativo</Text></>} /></ScreenContainer>;
}

const styles = StyleSheet.create({ content: { gap: 11, paddingBottom: 30, paddingTop: 8 }, header: { alignItems: "center", flexDirection: "row", justifyContent: "space-between", marginBottom: 4 }, headerTitle: { color: colors.ink, fontSize: 17, fontWeight: "800" }, back: { alignItems: "center", backgroundColor: colors.white, borderColor: colors.line, borderRadius: 13, borderWidth: 1, height: 42, justifyContent: "center", width: 42 }, edit: { alignItems: "center", backgroundColor: colors.mint, borderRadius: 13, height: 42, justifyContent: "center", width: 42 }, hero: { alignItems: "center", flexDirection: "row", gap: 13, padding: 14 }, photo: { alignItems: "center", backgroundColor: colors.mint, borderRadius: 16, height: 86, justifyContent: "center", overflow: "hidden", width: 86 }, image: { height: "100%", width: "100%" }, heroCopy: { flex: 1 }, code: { color: colors.green, fontSize: 9, fontWeight: "900", letterSpacing: 0.5 }, name: { color: colors.ink, fontSize: 17, fontWeight: "800", marginTop: 3 }, description: { color: colors.muted, fontSize: 11, lineHeight: 15, marginTop: 4 }, category: { color: colors.ink, fontSize: 10, fontWeight: "800", marginTop: 6 }, metrics: { flexDirection: "row", gap: 10 }, metric: { flex: 1, gap: 3, minHeight: 95 }, metricLabel: { color: colors.muted, fontSize: 10, fontWeight: "800" }, metricValue: { color: colors.green, fontSize: 23, fontWeight: "800" }, metricUnit: { fontSize: 11 }, metricPrice: { color: colors.ink, fontSize: 16, fontWeight: "800", marginTop: 4 }, metricHint: { color: colors.muted, fontSize: 10 }, lowStock: { color: colors.coral }, status: { alignItems: "center", backgroundColor: colors.white, borderColor: colors.line, borderRadius: 13, borderWidth: 1, flexDirection: "row", gap: 8, padding: 12 }, statusText: { color: colors.ink, fontSize: 11, fontWeight: "700" }, sectionTitle: { color: colors.ink, fontSize: 17, fontWeight: "800", marginTop: 5 }, movement: { alignItems: "center", backgroundColor: colors.white, borderColor: colors.line, borderRadius: 15, borderWidth: 1, flexDirection: "row", gap: 10, padding: 12 }, movementIcon: { alignItems: "center", borderRadius: 12, height: 39, justifyContent: "center", width: 39 }, inIcon: { backgroundColor: colors.mint }, outIcon: { backgroundColor: "#FDE9E4" }, movementCopy: { flex: 1 }, movementTitle: { color: colors.ink, fontSize: 12, fontWeight: "800" }, movementDate: { color: colors.muted, fontSize: 10, marginTop: 3 }, movementEnd: { alignItems: "flex-end", gap: 3 }, delta: { color: colors.green, fontSize: 11, fontWeight: "800" }, deltaOut: { color: colors.coral }, balance: { color: colors.muted, fontSize: 9, fontWeight: "700" }, empty: { alignItems: "center", gap: 8, paddingVertical: 35 }, emptyText: { color: colors.muted, fontSize: 12, fontWeight: "700", textAlign: "center" }, notFound: { alignItems: "center", flex: 1, gap: 11, justifyContent: "center" }, notFoundText: { color: colors.ink, fontSize: 16, fontWeight: "800" }, returnButton: { backgroundColor: colors.mint, borderRadius: 12, paddingHorizontal: 15, paddingVertical: 11 }, returnText: { color: colors.green, fontSize: 12, fontWeight: "800" }, pressed: { opacity: 0.72, transform: [{ scale: 0.97 }] } });

import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { router } from "expo-router";
import { useState } from "react";
import { Alert, FlatList, Pressable, StyleSheet, Switch, Text, TextInput, View } from "react-native";

import { Card, colors, PrimaryButton, SectionTitle } from "@/components/nexo-ui";
import { ScreenContainer } from "@/components/screen-container";
import { BUSINESS_PROFILES, useBusiness } from "@/lib/business-store";
import { haptic } from "@/lib/haptics";
import { useNexo } from "@/lib/pos-store";
import { getProfileCatalogImage, getProfileStarterCatalog } from "../shared/business-profile-content";
import { createProductCode } from "@/shared/product-code";
import type { BusinessFeatures, BusinessProfileDefinition } from "@/shared/business-types";

function FeatureToggle({ icon, label, value, onChange }: { icon: keyof typeof MaterialIcons.glyphMap; label: string; value: boolean; onChange: (value: boolean) => void }) {
  return <View style={styles.featureRow}><View style={styles.featureIcon}><MaterialIcons name={icon} size={17} color={colors.green} /></View><Text style={styles.featureLabel}>{label}</Text><Switch value={value} onValueChange={onChange} trackColor={{ false: "#D6DEDA", true: "#8FCEBA" }} thumbColor={value ? colors.green : colors.white} /></View>;
}

export default function BusinessProfileScreen() {
  const { configuration, profile, selectProfile, updateBusinessName, updateFeatures, updateCopy } = useBusiness();
  const { products, upsertImportedProducts } = useNexo();
  const [name, setName] = useState(configuration.businessName);
  const starterCatalog = getProfileStarterCatalog(profile.id);

  const featureChange = (feature: keyof BusinessFeatures, value: boolean) => updateFeatures({ [feature]: value });
  const renderProfile = ({ item }: { item: BusinessProfileDefinition }) => {
    const selected = item.id === profile.id;
    return <Pressable onPress={() => { haptic.medium(); selectProfile(item.id); }} style={({ pressed }) => [styles.profileCard, selected && styles.profileSelected, pressed && styles.pressed]}><View style={[styles.profileIcon, { backgroundColor: selected ? colors.green : colors.mint }]}><MaterialIcons name={item.icon} size={23} color={selected ? colors.white : colors.green} /></View><View style={styles.profileCopy}><Text style={styles.profileName}>{item.label}</Text><Text style={styles.profileDescription}>{item.description}</Text></View>{selected ? <MaterialIcons name="check-circle" size={21} color={colors.green} /> : <MaterialIcons name="chevron-right" size={21} color={colors.muted} />}</Pressable>;
  };

  const loadStarterCatalog = () => {
    if (!starterCatalog.length) return;
    Alert.alert("Cargar catálogo inicial", `Se crearán o actualizarán ${starterCatalog.length} elementos para ${profile.label}.`, [{ text: "Cancelar", style: "cancel" }, { text: "Cargar", onPress: () => {
      const imageUri = getProfileCatalogImage(profile.id);
      const items = starterCatalog.map((item, index) => ({ ...item, code: createProductCode(`${profile.shortLabel}-${item.name}`, products.length + index), imageUri, galleryImageUris: imageUri ? [imageUri] : undefined, price: item.price ?? 0, cost: item.cost ?? 0, stock: item.stock ?? (item.type === "SERVICE" ? 1 : 0), minStock: 0, showInCatalog: true }));
      const result = upsertImportedProducts(items, `Catálogo inicial · ${profile.label}`);
      haptic.success();
      Alert.alert("Catálogo cargado", `${result.created} elementos creados y ${result.updated} actualizados.`);
    } }]);
  };

  const loadProfileExamples = () => router.push("/profile-demo-editor" as never);

  return <ScreenContainer edges={["top", "bottom", "left", "right"]} containerClassName="bg-[#F6F3EE]" className="px-5"><FlatList data={BUSINESS_PROFILES} renderItem={renderProfile} keyExtractor={(item) => item.id} contentContainerStyle={styles.content} showsVerticalScrollIndicator={false} ListHeaderComponent={<><View style={styles.header}><Pressable onPress={() => router.back()} style={({ pressed }) => [styles.back, pressed && styles.pressed]}><MaterialIcons name="arrow-back" size={21} color={colors.ink} /></Pressable><View><Text style={styles.eyebrow}>ADMINISTRACIÓN</Text><Text style={styles.title}>Perfil de negocio</Text></View></View><Card style={styles.intro}><MaterialIcons name="business-center" size={22} color={colors.green} /><Text style={styles.introText}>Elige el perfil que mejor describe tu operación. Salud y veterinaria gestionan servicios y agenda comercial, no historias clínicas ni diagnósticos.</Text></Card><Text style={styles.label}>Nombre visible del negocio</Text><TextInput value={name} onChangeText={setName} onEndEditing={() => updateBusinessName(name)} placeholder="Nombre de tu negocio" placeholderTextColor={colors.muted} returnKeyType="done" style={styles.input} /><SectionTitle title="Tipo de negocio" /></>} ListFooterComponent={<View style={styles.footer}><SectionTitle title="Módulos activos" /><Card style={styles.features}><FeatureToggle icon="restaurant-menu" label="Recetas y productos compuestos" value={configuration.features.recipes} onChange={(value) => featureChange("recipes", value)} /><FeatureToggle icon="table-restaurant" label="Mesas y atención de salón" value={configuration.features.tables} onChange={(value) => featureChange("tables", value)} /><FeatureToggle icon="qr-code-scanner" label="Código de barras" value={configuration.features.barcode} onChange={(value) => featureChange("barcode", value)} /><FeatureToggle icon="sell" label="Precios mayoristas" value={configuration.features.wholesalePricing} onChange={(value) => featureChange("wholesalePricing", value)} /><FeatureToggle icon="two-wheeler" label="Delivery" value={configuration.features.delivery} onChange={(value) => featureChange("delivery", value)} /><FeatureToggle icon="storefront" label="Catálogo público" value={configuration.features.catalog} onChange={(value) => featureChange("catalog", value)} /><FeatureToggle icon="verified-user" label="Validación de edad" value={configuration.features.ageCheck} onChange={(value) => featureChange("ageCheck", value)} /><FeatureToggle icon="scale" label="Productos por peso" value={configuration.features.weightedProducts} onChange={(value) => featureChange("weightedProducts", value)} /><FeatureToggle icon="event-available" label="Agenda y solicitudes" value={configuration.features.appointments} onChange={(value) => featureChange("appointments", value)} /><FeatureToggle icon="assignment" label="Órdenes de servicio" value={configuration.features.serviceOrders} onChange={(value) => featureChange("serviceOrders", value)} /><FeatureToggle icon="straighten" label="Variantes por talla o referencia" value={configuration.features.variants} onChange={(value) => featureChange("variants", value)} /><FeatureToggle icon="language" label="Ventas y pedidos en línea" value={configuration.features.onlineSales} onChange={(value) => featureChange("onlineSales", value)} /></Card><SectionTitle title="Textos comerciales" /><Card style={styles.copyCard}><Text style={styles.copyLabel}>Mensaje del catálogo</Text><TextInput value={configuration.copy.catalogGreeting} onChangeText={(catalogGreeting) => updateCopy({ catalogGreeting })} multiline placeholder="Mensaje de bienvenida" placeholderTextColor={colors.muted} style={styles.copyInput} /><Text style={styles.copyLabel}>Mensaje del CRM</Text><TextInput value={configuration.copy.crmMessage} onChangeText={(crmMessage) => updateCopy({ crmMessage })} multiline placeholder="Contexto para el CRM" placeholderTextColor={colors.muted} style={styles.copyInput} /><Text style={styles.copyLabel}>Bienvenida del agente</Text><TextInput value={configuration.copy.agentWelcome} onChangeText={(agentWelcome) => updateCopy({ agentWelcome })} multiline placeholder="Orientación del agente" placeholderTextColor={colors.muted} style={styles.copyInput} /></Card><Pressable onPress={loadProfileExamples} style={({ pressed }) => [styles.categoriesLink, pressed && styles.pressed]}><View style={styles.categoryLinkIcon}><MaterialIcons name={profile.features.appointments ? "event-available" : "auto-awesome"} size={19} color={colors.green} /></View><View style={styles.categoryLinkCopy}><Text style={styles.categoryLinkTitle}>Cargar ejemplos de {profile.shortLabel}</Text><Text style={styles.categoryCaption}>{profile.features.appointments ? "Servicios, citas y solicitudes comerciales" : "Productos, pedidos y seguimientos comerciales"}</Text></View><MaterialIcons name="chevron-right" size={20} color={colors.muted} /></Pressable>{starterCatalog.length ? <Pressable onPress={loadStarterCatalog} style={({ pressed }) => [styles.categoriesLink, pressed && styles.pressed]}><View style={styles.categoryLinkIcon}><MaterialIcons name="playlist-add" size={19} color={colors.green} /></View><View style={styles.categoryLinkCopy}><Text style={styles.categoryLinkTitle}>Cargar catálogo inicial</Text><Text style={styles.categoryCaption}>{starterCatalog.length} servicios o productos sugeridos para {profile.label}</Text></View><MaterialIcons name="chevron-right" size={20} color={colors.muted} /></Pressable> : null}<Pressable onPress={() => router.push("/product-categories" as never)} style={({ pressed }) => [styles.categoriesLink, pressed && styles.pressed]}><View style={styles.categoryLinkIcon}><MaterialIcons name="category" size={19} color={colors.green} /></View><View style={styles.categoryLinkCopy}><Text style={styles.categoryLinkTitle}>Categorías de productos</Text><Text style={styles.categoryCaption}>{configuration.categories.length} categorías personalizadas</Text></View><MaterialIcons name="chevron-right" size={20} color={colors.muted} /></Pressable><PrimaryButton label="Guardar perfil" icon="check-circle" onPress={() => { updateBusinessName(name); haptic.success(); Alert.alert("Perfil actualizado", "La configuración se guardó y se aplicará a los módulos de NexoPOS.", [{ text: "Listo", onPress: () => router.back() }]); }} /></View>} /></ScreenContainer>;
}

const styles = StyleSheet.create({
  content: { gap: 10, paddingBottom: 18, paddingTop: 8 }, header: { alignItems: "center", flexDirection: "row", gap: 12, marginBottom: 4 }, back: { alignItems: "center", backgroundColor: colors.white, borderColor: colors.line, borderRadius: 13, borderWidth: 1, height: 42, justifyContent: "center", width: 42 }, eyebrow: { color: colors.muted, fontSize: 10, fontWeight: "800", letterSpacing: 0.7 }, title: { color: colors.ink, fontSize: 23, fontWeight: "800", letterSpacing: -0.5, marginTop: 2 }, intro: { alignItems: "center", backgroundColor: colors.mint, borderColor: "#B5E0CF", flexDirection: "row", gap: 10, padding: 13 }, introText: { color: colors.ink, flex: 1, fontSize: 11, lineHeight: 16 }, label: { color: colors.ink, fontSize: 12, fontWeight: "800", marginTop: 4 }, input: { backgroundColor: colors.white, borderColor: colors.line, borderRadius: 12, borderWidth: 1, color: colors.ink, fontSize: 14, fontWeight: "700", minHeight: 48, paddingHorizontal: 13 }, profileCard: { alignItems: "center", backgroundColor: colors.white, borderColor: colors.line, borderRadius: 16, borderWidth: 1, flexDirection: "row", gap: 11, minHeight: 75, padding: 11 }, profileSelected: { borderColor: colors.green, borderWidth: 1.5 }, profileIcon: { alignItems: "center", borderRadius: 13, height: 45, justifyContent: "center", width: 45 }, profileCopy: { flex: 1 }, profileName: { color: colors.ink, fontSize: 14, fontWeight: "800" }, profileDescription: { color: colors.muted, fontSize: 10, lineHeight: 14, marginTop: 3 }, footer: { gap: 12, marginTop: 4 }, features: { paddingBottom: 0, paddingTop: 0 }, featureRow: { alignItems: "center", borderBottomColor: colors.line, borderBottomWidth: 1, flexDirection: "row", gap: 9, minHeight: 56 }, featureIcon: { alignItems: "center", backgroundColor: colors.mint, borderRadius: 9, height: 30, justifyContent: "center", width: 30 }, featureLabel: { color: colors.ink, flex: 1, fontSize: 11, fontWeight: "700" }, copyCard: { gap: 6, padding: 12 }, copyLabel: { color: colors.ink, fontSize: 10, fontWeight: "800", marginTop: 3 }, copyInput: { backgroundColor: "#F8F8F6", borderColor: colors.line, borderRadius: 10, borderWidth: 1, color: colors.ink, fontSize: 11, lineHeight: 15, minHeight: 46, padding: 9, textAlignVertical: "top" }, categoryCaption: { color: colors.muted, fontSize: 10, lineHeight: 15 }, categoriesLink: { alignItems: "center", backgroundColor: colors.white, borderColor: colors.line, borderRadius: 14, borderWidth: 1, flexDirection: "row", gap: 10, minHeight: 64, padding: 11 }, categoryLinkIcon: { alignItems: "center", backgroundColor: colors.mint, borderRadius: 10, height: 36, justifyContent: "center", width: 36 }, categoryLinkCopy: { flex: 1 }, categoryLinkTitle: { color: colors.ink, fontSize: 12, fontWeight: "800" }, pressed: { opacity: 0.7, transform: [{ scale: 0.98 }] },
});
